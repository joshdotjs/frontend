- check if user already exists in DB
  -- if so, then return error message

- error handling for failed HTTP request


- Orders:
  -- Orders Route Backend:
    // TODO: Get Order by User ID
      --- Add testing

  -- Orders Page Frontend:
    // TODO: Change this to get Oders by User-ID
      --- Add testing
    // TODO: Add authorization to ensure only logged-in users can see their own orders
      --- Add testing


=================================================

Bugs:
  - navlinks need to be clicked more than once sometimes 
    -- the dropdown is much worse
  - Major MUI bug:
    -- Need to re-install node_modules (!?!?!?!?)